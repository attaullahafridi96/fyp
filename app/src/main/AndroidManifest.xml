<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.documentationrecordviafingerprint">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.samsung.android.providers.context.permission.WRITE_USE_APP_FEATURE_SURVEY" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.AppSettings"></activity>
        <activity android:name=".controller.UploadActivity" />
        <activity
            android:name=".controller.SearchActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".controller.TextEditorActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".internetchecking.NoInternetScreen" />
        <activity
            android:name=".userlogin.RegistrationForm"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".userlogin.Login" android:windowSoftInputMode="stateHidden">

        </activity>
        <activity android:name=".fingerprint.FingerprintAuthentication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.DashboardActivity">

        </activity>
        <activity android:name=".controller.OfflineFileViewer" />
        <activity android:name=".controller.OnlineFileViewer" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.android.documentationrecordviafingerprint.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- The below code is for android OS version below N -->
        <!--<receiver
            android:name=".internetchecking.ConnectivityReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"
                    tools:ignore="BatteryLife" />
            </intent-filter>
        </receiver>-->
    </application>

</manifest>